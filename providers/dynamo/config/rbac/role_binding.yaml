apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubeairunway-dynamo-provider-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kubeairunway-dynamo-provider-role
subjects:
- kind: ServiceAccount
  name: kubeairunway-dynamo-provider
  namespace: kubeairunway-system
