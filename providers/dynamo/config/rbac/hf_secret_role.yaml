apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: airunway-hf-secret
  namespace: dynamo-system
  labels:
    app.kubernetes.io/name: airunway
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["hf-token-secret"]
  verbs: ["delete"]
